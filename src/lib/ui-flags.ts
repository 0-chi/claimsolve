// 一時的な表示切替(コードは消さずに非表示にするための定数)。
// 会員が増えて閲覧権(期間制限)を再開するときは true に戻す。
// - SHOW_HERO_CTA: LPヒーロー直下のCTAボタン2つ(下部CTAは常時表示)
// - SHOW_VIEWPASS_UI: 閲覧権の期間・特典に関する文言/列/ボックス一式
export const SHOW_HERO_CTA = false;
export const SHOW_VIEWPASS_UI = false;
// 「書き方は4つ」表のカテゴリ名列(レビュー/記録/沈黙レポート/担当者への申し出)。
// データは残したまま表示だけ隠す。
export const SHOW_WRITE_TYPE_COLUMN = false;
